# Database settings: set real values in the service environment, never in source code.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=users_db
DB_USER=app_user
DB_PASSWORD=replace-with-a-real-password

# The application is always bound to 127.0.0.1.
APP_PORT=8000
LOG_LEVEL=INFO
